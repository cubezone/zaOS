library ActiveFormWebCamProj1;

uses
  ComServ,
  ActiveFormWebCamProj1_TLB in 'ActiveFormWebCamProj1_TLB.pas',
  ActiveFormWebCamImpl1 in 'ActiveFormWebCamImpl1.pas' {WebCam: TActiveForm} {ActiveFormWebCam: CoClass};

{$E ocx}

exports
  DllGetClassObject,
  DllCanUnloadNow,
  DllRegisterServer,
  DllUnregisterServer;

{$R *.TLB}

{$R *.RES}

begin
end.
